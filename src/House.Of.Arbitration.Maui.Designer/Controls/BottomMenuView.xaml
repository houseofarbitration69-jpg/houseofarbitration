<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:House.Of.Arbitration.Maui.Designer.Models"
             x:Class="House.Of.Arbitration.Maui.Designer.Controls.BottomMenuView"
             x:Name="ThisMenu">
    <Frame BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Black}}"
           CornerRadius="20"
           Margin="10"
           Padding="0"
           HasShadow="True"
           HeightRequest="75"
           VerticalOptions="End">
        <Grid x:Name="ContainerGrid">
            <!-- Dynamic Selection Indicator -->
            <BoxView x:Name="SelectionIndicator"
                     BackgroundColor="{StaticResource Primary}"
                     HeightRequest="3"
                     VerticalOptions="End"
                     Margin="15,0,15,5"
                     CornerRadius="2"
                     WidthRequest="50"
                     HorizontalOptions="Start"/>

            <!-- Bindable Layout for Menu Items -->
            <Grid x:Name="MenuGrid" 
                  BindableLayout.ItemsSource="{Binding MenuItems, Source={x:Reference ThisMenu}}"
                  Padding="5">
                <BindableLayout.ItemTemplate>
                    <DataTemplate x:DataType="models:MenuNavItem">
                        <VerticalStackLayout VerticalOptions="Center" Spacing="2">
                            <Label Text="{Binding Icon}" 
                                   FontFamily="FA-Solid"
                                   FontSize="24"
                                   HorizontalOptions="Center"
                                   TextColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource White}}" />
                            <Label Text="{Binding Title}" 
                                   FontSize="10" 
                                   HorizontalOptions="Center" />
                            <VerticalStackLayout.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnMenuItemTapped" CommandParameter="{Binding .}" />
                            </VerticalStackLayout.GestureRecognizers>
                        </VerticalStackLayout>
                    </DataTemplate>
                </BindableLayout.ItemTemplate>
            </Grid>
        </Grid>
    </Frame>
</ContentView>